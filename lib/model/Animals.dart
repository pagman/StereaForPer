class Animal {
  int id;
  String name;
  String description;
  String tag;
  String type;
  String image;
  double lat;
  double lon;

  Animal(this.id, this.name, this.description, this.tag, this.type, this.image, this.lat, this.lon);
}